spring:
  application:
    name: handy-server
  datasource:
    url: jdbc:mysql://localhost:3306/handy?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080
  address: 0.0.0.0

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

logging:
  level:
    root: INFO
    com.handy.appserver: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG 